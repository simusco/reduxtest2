import { connect } from 'react-redux';
import Test from '../components/keyword/Test';

const mapStateToProps = (state)=>{
    return {}
}

const mapDispatchToProps = (dispatch) => {
    return {}
}

export default connect(mapStateToProps,mapDispatchToProps)(Test);